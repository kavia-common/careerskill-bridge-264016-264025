# Backend service
PORT=3001
HOST=0.0.0.0

# URLs
BACKEND_URL=http://localhost:3001
FRONTEND_URL=http://localhost:3000
WS_URL=ws://localhost:3001/ws/notifications

# JWT secret (change in non-dev)
SECRET_KEY=CHANGE_ME_DEV_ONLY

# CORS
# Comma-separated; backend also augments with FRONTEND_URL and localhost:3000
CORS_ORIGINS=http://localhost:3000

# Optional fine-grained CORS
# ALLOWED_ORIGINS=http://localhost:3000
# ALLOWED_HEADERS=*
# ALLOWED_METHODS=*

# Database
DATABASE_URL=sqlite:///./app.db
