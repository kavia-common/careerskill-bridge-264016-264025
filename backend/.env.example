# SkillBridge Backend - Example environment variables
# Copy to backend/.env or project root .env and adjust values for your environment.

# Service networking
PORT=3001
HOST=0.0.0.0

# Public URLs (used in docs, CORS augmentation, and frontend config reference)
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:3001
WS_URL=ws://localhost:3001/ws/notifications

# CORS configuration
# Option 1: leave defaults (allows "*" and will auto-add FRONTEND_URL + localhost:3000)
# Option 2: restrict origins explicitly (comma-separated)
# CORS_ORIGINS=http://localhost:3000,https://your-frontend-host:3000
# ALLOWED_ORIGINS=http://localhost:3000

# Security
# NOTE: Replace in non-dev environments
SECRET_KEY=CHANGE_ME_FOR_NON_DEV

# Database (SQLite by default for dev)
DATABASE_URL=sqlite:///./app.db

# Frontend build variables (backend ignores these but they may exist in env)
REACT_APP_API_BASE=http://localhost:3001
REACT_APP_WS_URL=ws://localhost:3001/ws/notifications
