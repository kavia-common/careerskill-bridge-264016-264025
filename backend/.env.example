# Backend environment variables (example)
# Copy to .env and adjust appropriately.

# Security
SECRET_KEY=CHANGE_ME_SECURE_VALUE

# Database
# Default uses SQLite file in backend working dir
DATABASE_URL=sqlite:///./app.db

# CORS (comma-separated list). For dev you may use *
CORS_ORIGINS=*
